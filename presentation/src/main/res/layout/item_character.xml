<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/character_item_height"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingTop="@dimen/nav_header_logo_padding_horizontal"
    android:layout_marginHorizontal="@dimen/character_item_margin_horizontal"
    android:id="@+id/clCharacterItem">

    <View
        android:id="@+id/vTopEdge"
        android:layout_width="match_parent"
        android:layout_height="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@color/red"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        style="@style/ItemBackground"
        android:paddingHorizontal="@dimen/button_padding_horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:gravity="start|center_vertical"
        android:textStyle="bold"
        app:rippleColor="@color/red" />

</androidx.constraintlayout.widget.ConstraintLayout>